import React, {PropTypes} from 'react';

export default class Conversation extends React.Component {
  render(){
    return (
      <p>Eventually this will wrap all the DOM included in templates/room/show.html.eex</p>
    );
  }
}
